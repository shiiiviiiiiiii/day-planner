// Configuration for the Day Out Planner
export const CONFIG = {
  // The password required to access the admin panel (Ctrl + Shift + A or clicking the bottom-right dot)
  ADMIN_PASSWORD: "shivam0828",
};
